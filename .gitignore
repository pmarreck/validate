# Zig build artifacts
.zig-cache/
zig-out/
zig-cache/

# Nix
.direnv/

# Editor files
*.swp
*~

# Build artifacts
*.o
*.a

# macOS
.DS_Store

# Semantic search
.codescan/

# Cloned source for analysis
deps/*-src/
deps/*_src/
deps/*_source/

# Test-generated ffmpeg symlinks
scripts/ffmpeg-bin/
result/
result
CLAUDE.md
.serena
inbox
.worktrees/

# Side-effect of `zig build --fetch=all` (~380MB).
zig-pkg/
# Pre-upgrade lock backups.
flake.lock.bak_*
.git-old/
.claude/
result-win/
.watchmanconfig

# Spotlight orphan-tombstone case study: the 6.9GB binary index mirror is
# local-only quarantine data (pm 2026-06-10). CASE_STUDY.md + metadata.ndjson
# stay tracked; the raw indexes do not.
# (broadened) entire orphan-tombstone case study is local-only pending the
# "is this a separate integrity product?" decision — keep out of validate's tree.
case-studies/spotlight-orphan-tombstone-2026-06-09/
case-studies/spotlight*
